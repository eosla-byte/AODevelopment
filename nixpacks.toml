[phases.setup]
nixPkgs = ["python3", "postgresql", "jdk17"]
# aptPkgs removed to prefer Nix environment

[phases.build]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
